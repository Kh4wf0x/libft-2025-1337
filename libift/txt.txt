❌  proto type all function "if ()"




// void ft_memset (void *ptr,....
// char *str = ptr ;
unsigned char *p = ptr;
بدل ما نستخدم مباشرة

c
Copy code
ptr
داخل الحلقة.
خليني أشرحها لك بالعقل والمنطق خطوة بخطوة 👇

🧩 أولاً: نوع المؤشر void *
في الدالة memset، المعامل الأول هو:

c
Copy code
void *ptr
يعني:

هذا المؤشر لا يشير إلى نوع معيّن، فقط “يشير إلى شيء في الذاكرة”.

لا نقدر نعمل عليه عمليات حسابية (pointer arithmetic)، مثل:

c
Copy code
ptr++;   // ❌ غير مسموح
ptr[i];  // ❌ غير مسموح
*ptr = ...; // ❌ غير مسموح
السبب؟ لأنّ المترجم لا يعرف “كم حجم العنصر” الذي يشير إليه ptr.
(هل هو بايت؟ أم int؟ أم struct؟ لا يوجد معلومة.)

⚙️ الحل
نحوّل المؤشر إلى نوع يعرف المترجم حجمه — وهو unsigned char *.

c
Copy code
unsigned char *p = ptr;
بهذا الشكل:

نخبر المترجم أن كل خطوة p++ تعني التقدّم بمقدار بايت واحد.

🧩 تخيّل عندك صندوق فيه خانات صغيرة

كل خانة في هذا الصندوق اسمها bit (بت).
الكمبيوتر يخزن الأرقام داخل صناديق من هذه الخانات.

الآن:

كل 8 بتات = 1 بايت (byte).

والكمبيوتر لما يقول "unsigned char" يعني:

استخدم فقط صندوق فيه 8 خانات (بتات).👇 يعني باختصار:

char → غير مضمون (ممكن يكون signed أو unsigned حسب النظام)

signed char → مضمونة القيم من -128 إلى 127

unsigned char → مضمونة القيم من 0 إلى 255


type casting
big endian and little endian
strcut 